import styled from 'styled-components';
import { Grid } from '@material-ui/core';

export const Container = styled(Grid)``;

export const ContentPaperContiner = styled(Grid)`
  position: relative;
  top: -180px;
  height: 100vh;
  padding-right: 20px;
`;
